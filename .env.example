# 站点对外访问地址（可选）
#
# 不设置时的自动行为：
#   本地运行            → 用 config/site.json 里的 url
#   Vercel 部署         → 自动使用该次部署的域名（VERCEL_URL）
#
# 什么时候需要设置：
#   绑定正式域名后，在这里（或 Vercel 项目的环境变量里）填入正式域名，
#   让 canonical / sitemap / RSS 全部指向正式域名，而不是 xxx.vercel.app。
#
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
