# 站点对外访问地址（可选）
#
# 不设置时的自动行为（见 lib/site.ts 的 resolveSiteUrl）：
#   本地运行      → 用 config/site.json 里的 url
#   Vercel 部署   → 自动使用 Vercel 注入的生产域名（VERCEL_PROJECT_PRODUCTION_URL），
#                   其次用本次部署域名（VERCEL_URL）
#
# 什么时候需要设置：
#   绑定正式域名后，在 Vercel 项目的 Environment Variables 里填入正式域名，
#   让 canonical / sitemap / RSS / OG 全部指向正式域名，而不是 xxx.vercel.app。
#   这一项优先级最高，改完重新部署即可，不需要改代码。
#
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
